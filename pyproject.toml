[build-system]
# Minimum requirements for the build system to execute.
requires = [
    "packaging==20.5; platform_machine=='arm64'",  # macos M1
    "setuptools>=42", 
    "wheel",
    "Cython>=0.29.24",
    ]

[tool.black]
line-length = 88
